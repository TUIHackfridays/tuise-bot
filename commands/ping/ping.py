def pong():
    """Returns a flag (boolean) and the result (string).
    The flag (first returned argument) is true to make the bot to speak, the result that the bot will soeak is "pong".
    """
    
    return True, "pong"
